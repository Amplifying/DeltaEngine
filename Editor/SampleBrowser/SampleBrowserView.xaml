<UserControl
	x:Class="DeltaEngine.Editor.SampleBrowser.SampleBrowserView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
	xmlns:sampleBrowser="clr-namespace:DeltaEngine.Editor.SampleBrowser"
	x:Name="SampleBrowserUserControl"
	d:DesignWidth="400" d:DesignHeight="600"
	MinWidth="400" MinHeight="600"
	mc:Ignorable="d"
	ToolTip="Delta Engine Sample Browser">

	<UserControl.Resources>


		<Style TargetType="Label" x:Key="DeltaLabel">
			<Setter Property="FontSize" Value="16" />
			<Setter Property="FontFamily" Value="Arial" />
			<Setter Property="FontStyle" Value="Normal" />
			<Setter Property="Background" Value="#e2e2e2" />
			<Setter Property="Foreground" Value="#FF5D5D5D" />
		</Style>


		<Style TargetType="TextBox" x:Key="DeltaTextBox">
			<Setter Property="FontSize" Value="16" />
			<Setter Property="FontFamily" Value="Arial" />
			<Setter Property="FontStyle" Value="Normal" />
			<Setter Property="Background" Value="#FFFBFBFB" />
			<Setter Property="Foreground" Value="#FF5D5D5D" />
			<Setter Property="BorderBrush" Value="{x:Null}" />
		</Style>

		<Style TargetType="ComboBox" x:Key="DeltaComboBox">
			<Setter Property="FontSize" Value="16" />
			<Setter Property="FontFamily" Value="Arial" />
			<Setter Property="FontStyle" Value="Normal" />
			<Setter Property="Background" Value="#FFFBFBFB" />
			<Setter Property="Foreground" Value="#FF5D5D5D" />
		</Style>

		<Style TargetType="ListBox" x:Key="DeltaListBox">
			<Setter Property="FontSize" Value="16" />
			<Setter Property="FontFamily" Value="Arial" />
			<Setter Property="FontStyle" Value="Normal" />
			<Setter Property="Background" Value="#FFFBFBFB" />
			<Setter Property="Foreground" Value="#FF5D5D5D" />
		</Style>

		<Style TargetType="Button" x:Key="DeltaButton">
			<Setter Property="FontSize" Value="16" />
			<Setter Property="FontFamily" Value="Arial" />
			<Setter Property="FontStyle" Value="Normal" />
			<Setter Property="Background" Value="#FF9C9C9C" />
			<Setter Property="Foreground" Value="#FFFFFFFF" />
		</Style>

	</UserControl.Resources>

	<UserControl.Background>
		<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
			<GradientStop Color="Black" Offset="1" />
			<GradientStop Color="DimGray" />
		</LinearGradientBrush>
	</UserControl.Background>
	<Grid Background="#FFF4F4F4">
		<Grid.RowDefinitions>
			<RowDefinition Height="12"/>
			<RowDefinition Height="30"/>
			<RowDefinition Height="12"/>
			<RowDefinition/>
			<RowDefinition Height="12"/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="11"/>
			<ColumnDefinition Width="156*"/>
			<ColumnDefinition Width="22"/>
			<ColumnDefinition Width="34"/>
			<ColumnDefinition Width="5"/>
			<ColumnDefinition Width="119*"/>
			<ColumnDefinition Width="8"/>
			<ColumnDefinition Width="30"/>
			<ColumnDefinition Width="12"/>
		</Grid.ColumnDefinitions>

		<TextBox
				Name="SearchTextBox" TextWrapping="Wrap"
				Text="{Binding Path=SearchFilter, UpdateSourceTrigger=PropertyChanged}" 
				FontSize="16" ToolTip="Filter results to find samples more easily"
				GotMouseCapture="SearchTextBoxGotMouseCapture" Grid.Column="5" Grid.Row="1" Margin="2,0,0,0">
			<i:Interaction.Triggers>
				<i:EventTrigger EventName="TextChanged">
					<i:InvokeCommandAction Command="{Binding Path=OnSearchTextChanged}" />
				</i:EventTrigger>
			</i:Interaction.Triggers>
			<TextBox.Style>
				<Style
						TargetType="{x:Type TextBox}">
					<Setter Property="Template">
						<Setter.Value>
							<ControlTemplate TargetType="{x:Type TextBox}">
								<Grid>
									<ScrollViewer
											Margin="2,2,0,0"
											x:Name="PART_ContentHost"
											SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
											BorderBrush="#3E3E3E" Background="White" />
									<TextBlock
											Margin="5,3,0,0"
											x:Name="textBlock" Opacity="0.345" Text="Search"
											TextWrapping="Wrap" Visibility="Hidden" />
								</Grid>
								<ControlTemplate.Triggers>
									<MultiTrigger>
										<MultiTrigger.Conditions>
											<Condition Property="IsFocused" Value="False" />
											<Condition Property="Text" Value="" />
										</MultiTrigger.Conditions>
										<Setter Property="Visibility" TargetName="textBlock" Value="Visible" />
									</MultiTrigger>
								</ControlTemplate.Triggers>
							</ControlTemplate>
						</Setter.Value>
					</Setter>
				</Style>
			</TextBox.Style>
		</TextBox>
		<Image
				Source="../Images/Search.png"
				ToolTip="Filter the current content project to find specific items" Margin="2,0,0,0" Grid.Column="3" Grid.Row="1" />
		<Image
				Source="../Images/Remove.png"
				Width="11" Height="11" HorizontalAlignment="Right"
				ToolTip="Remove the filter text to see all items in the selected project again"
				VerticalAlignment="Top" Margin="0,10,7,0" Grid.Column="5" Grid.Row="1">
			<i:Interaction.Triggers>
				<i:EventTrigger EventName="MouseDown">
					<i:InvokeCommandAction Command="{Binding Path=OnSearchTextRemoved}" />
				</i:EventTrigger>
			</i:Interaction.Triggers>
		</Image>

		<ComboBox
			ItemsSource="{Binding Path=AssembliesAvailable}"
			SelectedValue="{Binding Path=SelectedAssembly, Mode=TwoWay}" Grid.Row="1" Grid.Column="1" Style="{StaticResource DeltaComboBox}">
			<i:Interaction.Triggers>
				<i:EventTrigger EventName="SelectionChanged">
					<i:InvokeCommandAction Command="{Binding Path=OnComboBoxSelectionChanged}" />
				</i:EventTrigger>
			</i:Interaction.Triggers>
		</ComboBox>
		<Grid
			Margin="0,3,2,0" VerticalAlignment="Top"
			HorizontalAlignment="Right" Grid.Column="5" Grid.Row="1" Height="0" Width="0"/>
		<ListBox
			Margin="0,0,-11,0" Background="#FFF4F4F4"
			MinWidth="400" HorizontalContentAlignment="Stretch"
			ItemsSource="{Binding Path=Samples}" Grid.ColumnSpan="8" Grid.Row="3" Grid.Column="1">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<sampleBrowser:SampleItemView Width="auto" DataContext="{Binding}" />
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
		<Button
			Content="?"
			Style="{StaticResource DeltaButton}" Grid.Column="7" Grid.Row="1" Foreground="White" FontSize="20" FontWeight="Bold">
			<Button.Background>
				<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
					<GradientStop Color="#FFA0A0A0" Offset="0"/>
					<GradientStop Color="#FF8B8B8B" Offset="1"/>
				</LinearGradientBrush>
			</Button.Background>
			<i:Interaction.Triggers>
				<i:EventTrigger EventName="Click">
					<i:InvokeCommandAction Command="{Binding Path=OnHelpClicked}" />
				</i:EventTrigger>
			</i:Interaction.Triggers>
		</Button>
	</Grid>
</UserControl>