<UserControl x:Class="DeltaEngine.Editor.ContentManager.ContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="24" d:DesignWidth="250">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="24" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Image Height="16" Width="16" VerticalAlignment="Top" 
			Source="{Binding Path=Icon}" Margin="4,4,4,0" />
		<TextBlock
			Grid.Column="1" FontSize="14" Margin="10,4,0,-2"
			HorizontalAlignment="Left" VerticalAlignment="Center" Width="206" Height="22"
			FontFamily="Arial" FontWeight="Bold" Text="{Binding Path=ContentName}"/>
	</Grid>
</UserControl>